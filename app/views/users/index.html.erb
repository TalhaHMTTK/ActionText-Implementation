<h1>hello </h1>

<% @users.each do |user| %>
    <p><%= user.name%></p>
    <p><%= user.notes %></p>

    <%= link_to 'Delete this User', user_path(user), method: :delete, data: { "turbo-method": :delete }, class: "btn btn-danger ml-3" %>
<% end %>